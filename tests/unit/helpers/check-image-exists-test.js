
import { checkImageExists } from 'blog/helpers/check-image-exists';
import { module, test } from 'qunit';

module('Unit | Helper | check image exists');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkImageExists([42]);
  assert.ok(result);
});

